
import { test } from '@playwright/test';
import { expect } from '@playwright/test';

test('mcp_docs_2025-10-26', async ({ page, context }) => {
  
    // Navigate to URL
    await page.goto('https://github.com/modelcontextprotocol/go-sdk');

    // Navigate to URL
    await page.goto('https://github.com/modelcontextprotocol/go-sdk');
});